def rewrite_content(text, tone="formal"):
    """
    Simple placeholder AI rewrite function.
    You can replace this with OpenAI API later.
    """

    # Simulated rewriting logic
    return f"[{tone.upper()} VERSION] {text}"
